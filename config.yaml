# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: olympus-protocol-metrics
description: Olympus protocol metrics for Arbitrum and Berachain

chains:
  - id: 42161 # Arbitrum One
    start_block: 10950000 # 2022-05-01
    rpc:
      - url: ${ARBITRUM_RPC_URL:-https://arbitrum.rpc.subquery.network/public}
        for: sync
      - url: ${ARBITRUM_FALLBACK_RPC_URL:-https://arbitrum-one.public.blastapi.io}
        for: sync
  - id: 80094 # Berachain
    start_block: 799194 # 2025-02-07 00:00:00 UTC
    rpc:
      - url: ${BERACHAIN_RPC_URL:-https://rpc.berachain.com}
        for: sync

full_batch_size: 1
address_format: lowercase
