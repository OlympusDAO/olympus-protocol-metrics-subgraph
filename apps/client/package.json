{
  "name": "@olympusdao/treasury-subgraph-client",
  "version": "3.0.0",
  "description": "Client for Olympus treasury metrics REST and legacy operations APIs.",
  "type": "module",
  "main": "./dist/apps/client/src/index.js",
  "types": "./dist/apps/client/src/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && pnpm run openapi:generate",
    "auth:login": "npm login --registry=https://registry.npmjs.org --scope=@olympusdao --userconfig ./.npmrc.local",
    "auth:whoami": "npm whoami --registry=https://registry.npmjs.org --userconfig ./.npmrc.local",
    "openapi:generate": "tsx scripts/write-openapi.ts",
    "pack:dry-run": "pnpm run build && npm pack --dry-run --ignore-scripts --cache /tmp/olympus-treasury-subgraph-npm-cache",
    "prepack": "pnpm run build",
    "prepublishOnly": "pnpm run release:check",
    "pretest": "pnpm run openapi:generate",
    "publish:client": "npm stage publish --access public --provenance --userconfig ./.npmrc.local --cache /tmp/olympus-treasury-subgraph-npm-cache",
    "release:check": "tsx scripts/release-check.ts",
    "stage:list": "npm stage list @olympusdao/treasury-subgraph-client --userconfig ./.npmrc.local",
    "test": "vitest run tests"
  },
  "files": [
    "CHANGELOG.md",
    "dist",
    "openapi.json"
  ],
  "exports": {
    ".": {
      "types": "./dist/apps/client/src/index.d.ts",
      "import": "./dist/apps/client/src/index.js"
    },
    "./openapi.json": "./openapi.json"
  },
  "sideEffects": false
}
